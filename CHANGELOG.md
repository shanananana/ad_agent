# 更新日志

## [0.1.4]（2026-03-27）

- 对话主链路收敛为「流式 + 思考展示」，去掉同步与其它流式对话接口。
- 编排侧并行拉取长短期记忆；意图结果优先按结构化 JSON 解析，失败再降级。
- 助手思考内容随会话持久化

## [0.1.3]（2026-03-25）
- **工具调用日志（状态机）**：注册主 `ToolCallingManager` Bean，委托 `DefaultToolCallingManager`，在每次工具执行前以 INFO 打印工具名与 JSON 参数（后续做状态机管理 在每一轮工具调用前做状态控制）。（2026-03-25）
- **意图识别 / 长期记忆判断**：使用无工具 `noToolChatClient`（专用 system），避免与主对话共用带工具的 `ChatClient` 导致误调 `queryBaseData`、幻觉 `u123` 等 userId。（2026-03-25）


## [0.1.2]（2026-03-24）

### 对话工作台（`/chat.html`） +  创意素材生成(生图)（2026-03-24）
- **提示词外置**：主对话 / 调价 / 创意 / 意图识别 / 长期记忆 / 编排器 user 骨架 / 版位说明 / 规划层展示文案等统一放在 `src/main/resources/prompts/`，由 `ClasspathPromptLoader` 加载；Java 仅注入占位符与业务数据。（2026-03-24）
- 左侧 Tab：**对话 Agent**、**计划查看**、**效果查看**、**调价日志**、**素材生成**；与头部 **用户 ID**、侧栏 **当前计划** 联动。（2026-03-23）
- **素材生成(自动生图)**：选广告组、可选内容库与**版位**；根据广告内容智能生成图片生成描述的prompt模板 然后使用优化后的prompt生成图片（2026-03-23）
- **智能生成画面描述**：结合高 ROI 素材文案、内容库摘要、文本框草稿，由大模型生成描述；**版位**决定提示模板里注入的画幅与安全区说明；`PromptTemplate` 拼装；生成结果**按语义换行**写入描述框；按钮紧挨画面描述输入区右侧，便于发现。（2026-03-23）
- **计划树 → 效果**：`chat.html` 结构树中素材为可点击按钮，跳转 **效果查看** Tab，按素材 ID 筛表明细，汇总卡片为筛选后数据；可「显示全部明细」；地址栏支持 `#tab=effects&creativeId=`。（2026-03-23）
- **README**：对话工作台 **素材生成** 配图使用 `docs/images/chat-material-gen.png`（由原 `text_2_png.png` 重命名）；**计划查看** 暂为文字说明。（2026-03-20 / 2026-03-23）

## [0.1.1]（2026-03-22）

### 效果看板（`/bid-strategy.html`）（2026-03-22）

- 按日趋势图、整计划/按行维度；调价成功日竖线参考；明细表迷你折线、冻结首列、指标格式优化。（2026-03-22）

### 自动调价 B×α（2026-03-20）

- 定时任务、按计划目录维护出价模型、系数、效果快照与任务日志。（2026-03-20）

### 对话与隐私（2026-03-20）

- 意图识别支持结合最近对话识别短句确认；可清除长期记忆与聊天记录；对话页用户 ID 校验等。（2026-03-20）

### 项目初始化（2026-03-17）

- Spring Boot + Spring AI 对话式投放 Agent；短/长期记忆与聊天记录；SSE 流式回复；本地数据目录约定。（2026-03-17）
