# README 配图

本目录存放根目录 `README.md` 中引用的 **对话工作台（`chat.html`）** 截图。

| 文件 | 说明 |
|------|------|
| `chatAgent.png` | 对话 Agent：流式回复与「思考过程」等 |
| `text_2_png.png` | **计划查看** Tab：计划表 + 结构树（素材可点击跳转效果页） |
| `job.png` | **调价日志** Tab 界面 |

更新截图时覆盖同名文件即可。

根目录 `README.md` 中配图使用 `<details open>` / `<summary>`：**默认展开显示图片，点击标题可收起隐藏**（GitHub 网页端支持；部分本地预览器可能表现不一致）。
