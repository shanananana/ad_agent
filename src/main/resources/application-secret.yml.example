# 复制为 application-secret.yml 并填写（已 gitignore）
# 对话与文生图均走 OpenAI 兼容 API，改 base-url / model / api-key 即可

spring:
  ai:
    openai:
      api-key: your-openai-compatible-api-key
      # base-url: https://api.minimaxi.com
      # chat:
      #   options:
      #     model: MiniMax-M3
